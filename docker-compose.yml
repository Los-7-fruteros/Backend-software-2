services:
  api:
    build: .
    container_name: backend-monitoreo
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/code         # ← hot reload en desarrollo
    restart: unless-stopped